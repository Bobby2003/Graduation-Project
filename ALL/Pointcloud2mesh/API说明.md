创建 pipeline	               RealtimeMappingPipeline(cfg)
启动外部喂帧模式	            pipeline.start_external()
提交修补后的 RGBD	            pipeline.submit_rgbd(color, depth, mask, frame_id, timestamp, extras)
提交完整 RGBDFrame	            pipeline.submit_frame(frame)
查询整体状态	                pipeline.get_status()
查询 tracking 统计	            pipeline.get_tracking_stats()
查询 mapping 统计	            pipeline.get_mapping_stats()
获取最新 tracking	            pipeline.get_latest_tracking()
获取最新 map snapshot	        pipeline.get_latest_map()
获取最新 mesh	                pipeline.get_latest_mesh()
获取 world 到 camera 外参	    pipeline.get_current_extrinsic_world_to_camera()
获取 camera 到 world 位姿	    pipeline.get_current_camera_pose_world()
获取 IMU 到 world 位姿	        pipeline.get_current_imu_pose_world()
获取相机/IMU 坐标系	            pipeline.get_current_coordinate_frames()
获取首帧 IMU 初始化信息	        pipeline.get_imu_world_init_info()
保存 mesh	                    pipeline.save_mesh(prefix)
重置当前建图	                pipeline.reset_reconstruction()
保存后重置建图	                pipeline.reset_reconstruction_and_save(prefix)
清空 local map	               pipeline.clear_local_map()
重置 tracker	               pipeline.reset_tracker()
停止并保存	                    pipeline.stop(save_mesh=True)
关闭不保存	                    pipeline.close()

调用的话from pipeline_api import RealtimeMappingPipeline就行了，见main_pipeline